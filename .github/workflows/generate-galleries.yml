name: Generate Galleries (disabled)

on:
  workflow_dispatch: {}

# Automated gallery generation has been disabled per repository owner request.
# Use manual dispatch or update the collections HTML files directly when needed.

jobs:
  manual-only:
    name: Manual-only placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Automated gallery generation is disabled."
          echo "To regenerate galleries, run this workflow manually or update collection files directly."
